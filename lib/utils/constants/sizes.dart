import 'package:flutter/material.dart';

class TSizes {
  /// -- Padding and margin sizes
  // Logo margin
  static const EdgeInsets authScreenLogoMargin = EdgeInsets.only(
    top: 30,
    bottom: 20,
    left: 20,
    right: 20,
  );
  // General Padding/Margins for widgets
  static const EdgeInsets generalPadding = EdgeInsets.all(16);
  static const EdgeInsets generalMargin = EdgeInsets.all(20);
  static const double generalSizedBoxHeight = 12;

  // Font sizes

  // Icon sizes

  // Button sizes

  // AppBar Height

  // Image sizes

  // Default spacing between sections

  // Border radius

  // Input field

  // Card sizes

  // Loading indicator size

  // Grid view spacing
}
